- hosts: portainer_slaves
  roles:
  - role: dockercontainer
    container_name: "portainer-agent_1.5.1"
    container_image: "portainer/agent:1.5.1"
    exposed_ports: "9001:9001"
    mounts:
        - { src: /var/run/docker.sock, dest: /var/run/docker.sock, type: sock }